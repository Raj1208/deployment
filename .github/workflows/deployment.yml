name: Pull and Use Docker Image

on:
  repository_dispatch:
    types: [image-updated]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull Docker Image
        run: docker pull raj456/homzy:latest
      - name: Commit SHA Digest File
        run: |
         echo "Adding SHA Digest file"
         touch 1.txt
         git config --global user.email "rajchauhan1208@gmail.com"
         git config --global user.name "Raj1208"
         git add 1.txt # Full path for the artifact
         git commit -m "Add SHA Digest file"
    
         git -c http.extraheader="AUTHORIZATION: bearer ${{ secrets.ARGOCD_PAT }}" push

